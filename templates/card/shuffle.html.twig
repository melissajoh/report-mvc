{% extends "base.html.twig" %}

{% block title %}Shuffled card deck{% endblock %}

{% block body %}
    <h1>Shuffled card deck</h1>
    <div class="carddeck">
        {% for card in shuffled %}
            <span class="cards {{ card }}">{{ card }}</span>
        {% endfor %}
    </div>
{% endblock %}
